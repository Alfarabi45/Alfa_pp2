#include <iostream>
#include <climits>
using namespace std;

int main() {
    int n; 
    cin >> n;

    long long max_val = LLONG_MIN;
    long long max2_val = LLONG_MIN;
    bool found_second = false;

    for (int i = 0; i < n * n; i++) {
        long long x; 
        cin >> x;
        if (x > max_val) {
            max2_val = max_val;
            max_val = x;
        } else if (x < max_val && x > max2_val) {
            max2_val = x;
            found_max2= true;
        }
    }

    if (!found_max2) cout << 0 << endl;
    else cout << max2_val << endl;

    return 0;
}











